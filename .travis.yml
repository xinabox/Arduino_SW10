language: python
python:
    - "2.7"
    
env:
    - PLATFORMIO_CI_SRC=examples/SW10_Temperature_Measurement/SW10_Temperature_Measurement.ino

install:
    - python -c "$(curl -fsSL https://raw.githubusercontent.com/platformio/platformio/master/scripts/get-platformio.py)"
    - pip install -U platformio

    # download library to the temporary directory
    - wget https://github.com/xinabox/arduino-CORE/archive/master.zip -O /tmp/arduino-CORE.zip
    - unzip /tmp/arduino-CORE.zip -d /tmp/

script:
    - platformio ci --lib="." --lib="/tmp/arduino-CORE-master" -b=uno -b=zero -b=xinabox_cw01 -b=xinabox_cw02

